{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cargo build --example midi --release",
            "command": "cargo build --example midi --release",
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build"
        },
        {
            "type": "shell",
            "label": "cargo build --example midi --release --features=\"itm-trace\"",
            "command": "cargo build --example midi --release --features=\"itm-trace\"",
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build"
        }
    ]
}